name: Full workflow

on:
  push:
    branches:
      - master
      - develop
      - "feature/**"

jobs:
  production:
    if: github.ref_name == 'master'
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/reviewed_deployment.yml
    with:
      environment: production
    secrets: inherit

  staging:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/standard_workflow.yml
    with:
      environment: staging
    secrets: inherit
